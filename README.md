# multimodal_curriculum

This repository contains a way of creating multi-modal curricula for the training of neural networks (i.e, biasing sampling of training batch from regions of high (or low depending on forward or reverse curriculum) probability mass for multimodal distributions).
